# PH15 - Dynamic Role Selection Login & Module Management System

## Overview
This phase implements three user-requested features:
1. **Dynamic role selection at login** — roles are hidden until credentials are validated; only roles assigned to the authenticated user are shown.
2. **Multi-role user support** — a single user can be assigned multiple roles and explicitly chooses which role to use each session.
3. **Module management system** — SuperAdmin can enable/disable modules globally; both SuperAdmin and Admin can configure per-user module access.

---

## Feature 1 & 2: Dynamic Role Selection Login (Two-Step UX)

### Problem
The original login form showed a static role dropdown listing all system roles before the user entered credentials. This leaked role names and didn't support per-user role assignment or selection.

### Solution: AJAX Two-Step Login Flow

**Step 1 — Credentials only:**
- User enters email and password and clicks **Continue**.
- An AJAX POST is sent to `/Account/ValidateCredentials`.
- The server validates the password (without signing in) via `UserManager.CheckPasswordAsync`.
- Returns `{ success: true, roles: ["Admin", "Doctor"] }` or `{ success: false }`.
- If successful, the credential fields are hidden and the role selector appears.

**Step 2 — Role selection:**
- The `<select>` is dynamically populated with only the user's assigned roles.
- If only one role is available it is pre-selected.
- User clicks **Sign In** which submits the full form (email, password, selectedRole).

### Files Changed

| File | Change |
|------|--------|
| `ViewModels/AccountViewModels.cs` | Added `SelectedRole` (optional string) to `LoginViewModel` |
| `Controllers/AccountController.cs` | Added `ValidateCredentials` AJAX endpoint; updated `Login` POST to validate role + write to session; updated `RedirectToLocalAsync` to accept selected role; added `PickPrimaryRole` fallback helper; `Logout` clears session key |
| `Views/Account/Login.cshtml` | Full two-step Razor view with JavaScript orchestration, antiforgery token injection via `@inject IAntiforgery`, spinner, back button, per-step error boxes, password visibility toggle |

### Key Code: ValidateCredentials Endpoint
```csharp
[HttpPost, AllowAnonymous, ValidateAntiForgeryToken]
public async Task<IActionResult> ValidateCredentials([FromForm] string email, [FromForm] string password)
{
    var user = await _userManager.FindByEmailAsync(email);
    if (user == null || !await _userManager.CheckPasswordAsync(user, password))
        return Json(new { success = false });
    var roles = await _userManager.GetRolesAsync(user);
    return Json(new { success = true, roles });
}
```

### Key Code: Active Role Session Storage
```csharp
HttpContext.Session.SetString("ActiveRole", selectedRole);
```

### Role Priority Fallback (PickPrimaryRole)
If the user bypasses role selection, `PickPrimaryRole` applies this priority order:
`SuperAdmin → Admin → Doctor → Nurse → Pharmacist → Accountant → Receptionist → Patient`

---

## Feature 3: Module Management System

### Problem
No mechanism existed to turn off system modules globally or restrict modules per user.

### Data Model

**`SystemModule`** (in `Models/RBAC.cs`):
```
Id, Key (unique), DisplayName, Icon, Description, IsGloballyEnabled (default true), SortOrder
```

**`UserModuleAccess`** (in `Models/RBAC.cs`):
```
Id, UserId (FK), ModuleId (FK), IsEnabled, UpdatedAt, UpdatedByUserId
Unique index on (UserId, ModuleId)
```

### Database Tables (Created at Startup)
`EnsureModuleTablesAsync()` in `DatabaseInitializer.cs` creates `SystemModules` and `UserModuleAccesses` using raw SQL (`IF NOT EXISTS`).

`SeedSystemModulesAsync()` inserts 23 default modules if the table is empty:

| Key | Display Name |
|-----|-------------|
| Dashboard | Dashboard |
| Patient | Patient Management |
| OPD | OPD (Outpatient) |
| IPD | IPD (Inpatient) |
| Billing | Billing & Payments |
| Lab | Laboratory |
| Radiology | Radiology |
| Pharmacy | Pharmacy |
| BloodBank | Blood Bank |
| Ambulance | Ambulance |
| OT | Operation Theatre |
| Referral | Referral & TPA |
| HRAttendance | HR - Attendance |
| HRLeave | HR - Leave |
| HRPayroll | HR - Payroll |
| FrontOffice | Front Office |
| Certificates | Certificates & IDs |
| CMS | CMS & Public Portal |
| Chatbot | AI Chatbot |
| ChatbotAdmin | Chatbot Admin |
| License | Licensing |
| Audit | Audit Logs |
| Reports | Reports & Exports |

### Business Rule
A user cannot have a globally-disabled module individually enabled. `ModuleService.SetUserModuleAccessAsync` returns an error if this is attempted.

### Access Control

| Action | Allowed Roles |
|--------|--------------|
| Toggle module globally | SuperAdmin only |
| View user list for module config | Admin, SuperAdmin |
| View/edit per-user module access | Admin, SuperAdmin |

### New Files

| File | Purpose |
|------|---------|
| `Services/Interfaces/IServices.cs` | Added `IModuleService` interface + `UserModuleAccessRow` DTO |
| `Services/Implementations/ModuleService.cs` | Full implementation of module enable/disable logic |
| `ViewModels/ModuleManagementViewModels.cs` | `SystemModuleListViewModel`, `UserListForModulesViewModel`, `UserSummary`, `UserModuleAccessViewModel` |
| `Controllers/ModuleManagementController.cs` | 5 actions: `Index`, `ToggleGlobal`, `Users`, `UserAccess`, `SetUserAccess` |
| `Views/ModuleManagement/Index.cshtml` | Toggle switch table for global module control |
| `Views/ModuleManagement/Users.cshtml` | Searchable user list with "Configure" button per user |
| `Views/ModuleManagement/UserAccess.cshtml` | Per-user module grid; globally-disabled rows grayed out |

### Service Registration
```csharp
// Program.cs
builder.Services.AddScoped<IModuleService, ModuleService>();
```

### AJAX API Endpoints (JSON body)

| Endpoint | Roles | Body |
|----------|-------|------|
| `POST /ModuleManagement/ToggleGlobal` | SuperAdmin | `{ "moduleId": 1, "isEnabled": false }` |
| `POST /ModuleManagement/SetUserAccess` | Admin, SuperAdmin | `{ "userId": "...", "moduleId": 1, "isEnabled": true }` |

---

## Build Status
- **Result:** Build succeeded (0 errors)
- Pre-existing warnings remain at ~1074 (unchanged)
- Bug fixed during implementation: `Views/ModuleManagement/Users.cshtml` line 56 used curly/smart quotes in a Razor C# expression; replaced with escaped straight quotes.

---

## Testing Notes
- Login: enter valid credentials → Continue → roles appear → select role → Sign In. Only roles assigned to the user in `UserRoles` will appear.
- Module management: navigate to `/ModuleManagement` (SuperAdmin) or `/ModuleManagement/Users` (Admin/SuperAdmin).
- Globally disabled modules show as grayed-out toggle in the per-user view and cannot be overridden.
